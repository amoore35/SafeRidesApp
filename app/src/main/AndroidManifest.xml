<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.elon.saferides" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".RiderInputActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RiderInfoCheckActivity"
            android:label="@string/title_activity_rider_info_check" >
        </activity>
        <activity
            android:name=".DriverCallerInfoActivity"
            android:label="@string/title_activity_driver_caller_info" >
        </activity>
    </application>

</manifest>
